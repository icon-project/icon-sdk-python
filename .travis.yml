language: python
python:
  - "3.6"

script:
- python setup.py test

deploy:
  provider: pypi
  distributions: sdist bdist_wheel
  #server: https://test.pypi.org/legacy/
  user: iconfoundation
  password: $Pypi_pw
  on:
    branch: master
